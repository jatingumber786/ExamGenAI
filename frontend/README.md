# Frontend Folder

This folder contains the UI layer used by the Flask app:

- `templates/` for Jinja page templates
- `static/` for CSS and browser-side JavaScript

If you later move to a separate frontend deployment, this is the side that should be replaced by a standalone app such as React, Next.js, or another client framework.
