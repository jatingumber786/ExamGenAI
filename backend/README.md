# Backend Folder

This folder contains the Flask backend:

- `app.py` for routes, database access, and business logic

The root `app.py` remains as a small compatibility entrypoint so local runs and existing deployment commands continue to work.
